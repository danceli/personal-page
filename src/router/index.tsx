import { BrowserRouter } from "react-router-dom"
import Routes from './modules';

const Router = () => {
  return (
    <BrowserRouter>
      <Routes />
    </BrowserRouter>
  )
}

export default Router;